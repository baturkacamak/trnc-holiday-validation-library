const HolidayValidator = require('../HolidayValidator');

/**
 * TurkeyHolidayValidator
 *
 * This class is used to determine if a given date is a holiday in Turkey.
 */
class TurkeyHolidayValidator extends HolidayValidator {
    // Uses the common holidays defined in the base class
}

module.exports = TurkeyHolidayValidator;
